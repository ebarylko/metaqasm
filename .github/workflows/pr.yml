on: pull_request
name: run_tests
jobs:
  setup_project:
    name: Setup metaQASM and run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: haskell-actions/setup@v2
        with:
          enable-stack: true
      - run: stack test
